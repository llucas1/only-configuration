-- CreateTable
CREATE TABLE "locadora" (
    "id" INTEGER NOT NULL,
    "name" TEXT NOT NULL,
    "data_cpf" TEXT NOT NULL,
    "move" TEXT NOT NULL,

    CONSTRAINT "locadora_pkey" PRIMARY KEY ("id")
);
