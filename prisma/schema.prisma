generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model locadora {
  id    Int     @id
  name  String
  data_cpf String
  move  String
}
